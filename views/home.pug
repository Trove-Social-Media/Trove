h1 Home Page 
  
form(method='POST' action='/catalog/home/create')
    div.form-group
        label(for='postcontent') Post Content:
        input#username.form-control(type='text', placeholder='What\'s on your mind?' name='postcontent' value=(undefined===userinfo ? '' : userinfo.username))
    button.btn.btn-primary(type='submit') Submit
    
a(href='/catalog/profile') Go to profile page

h1 Feed

ul
each element in post_list
    form(method='POST' action='/catalog/home/like')
        div.form-group
        li= element
        br
        button.btn.btn-primary(type='submit' value=element._id name="action") Like post that says  #{element.content}
        br
    
    form(method='POST' action='/catalog/home/comment')
        div.form-group
        br
        button.btn.btn-primary(type='submit' value=element._id name="action") Add a fake comment to a post that says  #{element.content}
        br
    
br
a(href='/catalog/userlist') Users List
